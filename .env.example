# Database Configuration
# ======================

# Database mode is automatically determined by NODE_ENV:
# - NODE_ENV=dev  → Uses local JSON files (./localData/)
# - NODE_ENV=prod → Uses MongoDB (requires MONGODB_URI)
#
# Optional: Set DB_MODE to manually override automatic detection
# DB_MODE=local  # Force local mode
# DB_MODE=mongodb  # Force MongoDB mode

# MongoDB connection string (required when NODE_ENV=prod or DB_MODE=mongodb)
# MONGODB_URI=mongodb://localhost:27017/store-manager
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/store-manager
